# Python ML stack — use a venv:  cd ml-service && python -m venv .venv
# Windows: .venv\Scripts\activate   |   Unix: source .venv/bin/activate
# pip install -r requirements.txt
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
numpy>=2.0.0
pandas>=2.2.0
torch>=2.5.0
yfinance>=0.2.40
