/**
 * Printed when using `npm run dev:vite` alone — Vite proxies /api to 127.0.0.1:3001.
 */
console.warn(
  '[suplexity] dev:vite: historic API is not started. For /api/market/bars run in another terminal: npm run server:historic — or use: npm run dev (starts both).\n',
)
